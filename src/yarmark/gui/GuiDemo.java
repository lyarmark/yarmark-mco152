package yarmark.gui;

public class GuiDemo {
	public static void main(String[] args) {

		// do not create a JFrame object
		// instead, extend the JFrame class and create an instance
		// you will customize the class in the JFrame class

		//JFrameDemo frame = new JFrameDemo();

		SmileJFrame frame = new SmileJFrame();
		frame.setVisible(true);

		/* draw arc of path
		 * start projectile at time = 0
		 * draw line from t=0 to t=1 to t=2 and so on
		 *
		 * projectile frame
		 * projectile component
		 * which will draw the graph
		 *
		 * as if it was a math class- (0,0) is bottom left
		 *
		 *a=31
		 *v=20
		 *if it works
		 *
		 *hard coded values
		 *draw in loop
		 */
	}
}
