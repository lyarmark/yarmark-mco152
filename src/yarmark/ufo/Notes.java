package yarmark.ufo;

public class Notes {
	/*
	 * gson works like this
	 * give gson the list and the top level class
	 * ex: UFOSightingList list =  gson.fromJson(in,  UFOSightingList.class);
	 * and it just works
	 * another way to import the list with gson:
	 * UFOSightings array[] = gson.fromJson(in, UFOSightings[].class);
	 *
	 * variables must match content in list (like parameter signature)
	 *
	 * hwmk:
	 * find top 10 UFO sighting locations
	 * in order of number
	 * and the number of sightings per location
	 */
}
